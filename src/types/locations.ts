export type LocationCardProps = {
    image: {
        url: string;
        isLeft?: boolean;
    }
    title: string;
    address: string;
    telephone: string;
    schedule: string;
    email: string;
}